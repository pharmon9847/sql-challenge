--perform crud commands
--check the departments table to make sure csv imported
select * from departments;

--check the dept_emp table to make sure csv imported 
select * from dept_emp;

--check the dept_manager table to make sure csv imported
select * from dept_manager;

--check the employees table to make sure the csv imported
select * from employees;

--check the salaries table to make sure the csv imported
select * from salaries;

--check the titles table to make sure the csv imported
select * from titles;